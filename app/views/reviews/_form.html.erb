<div class="container form-container">
  <%= simple_form_for [ @restaurant, @review ] do |r| %>
    <%= r.input :rating %>
    <%= r.input :content %>
    <%= r.submit "Submit review" %>
  <% end %>
</div>
